-- database: Gizli Dosya Patikası


SELECT * FROM movie
where YEAR = 1982;

SELECT AVG(score) FROM movie
where score > (SELECT AVG(score) from movie)
ORDER by score DESC;

--Bu bir yorum satırı dsfojkdkşj
